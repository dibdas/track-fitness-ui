import React from 'react';
import PropTypes from 'prop-types';

const MeasurementTrack = ({ measurement, exercises }) =>

return(
<>

  {exercises.map((exercise)=>(
            exercise.id
            ))}


)

  </>
);
